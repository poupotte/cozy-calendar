// Generated by CoffeeScript 1.8.0
var Folder;

Folder = require('../models/folder');

module.exports.getUrl = function(req, res) {
  var path;
  path = req.params.path;
  path = path.replace('.', '/');
  path = path.replace('.', '/');
  return Folder.request('byFullPath', {
    key: "/" + path
  }, function(err, folders) {
    if (err) {
      return res.send({
        error: err
      }, 500);
    }
    if (folders.length === 0) {
      return res.send("Folder not found", 404);
    } else {
      return res.send("files/folders/" + folders[0].id);
    }
  });
};
